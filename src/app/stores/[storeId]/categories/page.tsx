// TODO: remove

export default function Page() {
  // export default function Page({ params }: { params: { storeId: string } }) {
  // const a = api.category.get.useQuery();

  return <div className="hidden lg:block">Nothing...</div>;
}
